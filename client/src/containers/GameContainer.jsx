import React, { PureComponent } from 'react';
import { Link } from 'react-router';

export default class GameListManager extends PureComponent {
   render () {
      return (
         <div className="container">
            item album
         </div>
      );
   }
}
